version: '3.8'

services:
  vibra-app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: vibra-music-app
    restart: unless-stopped
    ports:
      - "5173:5173"
    env_file:
      - .env.local
    environment:
      - NODE_ENV=production
